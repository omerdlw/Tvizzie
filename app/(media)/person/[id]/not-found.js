import NotFoundTemplate from '@/features/app-shell/not-found-template';

export default function NotFound() {
  return (
    <NotFoundTemplate description="We couldn't find this person. The profile may have been removed, or the link may be invalid" />
  );
}
