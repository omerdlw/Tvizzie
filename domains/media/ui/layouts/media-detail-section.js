'use client';

export const MEDIA_DETAIL_SECTION_HEADER_CLASS =
  'mb-4 flex w-full items-center justify-between gap-4';

export const MEDIA_DETAIL_SECTION_CONTENT_CLASS = 'w-full';

// The detail page uses a single vertical rhythm: 32px on compact layouts,
// 40px on small screens, and 48px from large screens onward. Keeping it in
// one place prevents sections from gradually drifting out of alignment.
export const MEDIA_DETAIL_STACK_CLASS = 'flex w-full flex-col gap-8 sm:gap-10 lg:gap-12';
